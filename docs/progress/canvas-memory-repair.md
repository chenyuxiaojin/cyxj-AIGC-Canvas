# 画布内存修复验收

## 基线与授权边界

修复分支基于正式安装对应的 `bbba1c9`。既有 Omni Flash 未提交能力在独立基线提交 `61866e5` 保留；原工程与旧工作树未改。本轮不调用真实模型或付费媒体生成，保留生成直接执行的产品行为。

现场报告记录 WebKit footprint 峰值 29.9 GiB，系统 swap 约 13–15 GiB；调用栈为 IndexedDB put/IPC/memmove。这证明数据复制放大，未证明传统不可回收泄漏。本轮未重启旧版本复现。

## 数据保护

完整 App、Application Support、WebKit ZIP 均已校验；另有包含 WAL 的 SQLite 与 IndexedDB 一致快照。根目录：`/Users/chenhuajin/项目/自己的应用/infinite-canvas-backups/memory-repair-20260920/`。

迁移脚本逐个 SQLite BLOB / UTF-16 字符串流式读取，单张原图解码、SHA-256 核对，按原有 `project-media/owned`、`local-ref` 格式保存。当前正文、两份持久历史、缓存和保存日志分别转换，绝不用后端覆盖草稿。旧全局缓存中目标项目无内嵌图，保留原样。其他 30 个项目逐字节一致。

隔离迁移：108 节点、92 连线、84 张原图共 148,329,947 字节；当前项目 396,257,940 → 793,156 字节。两个 IndexedDB 记录由约 792 MB 降到约 1.586 MB（UTF-16）。五份记录的非媒体结构、未知字段与逐图哈希一致；业务数据库完整性与 IndexedDB quick_check 为 ok。IndexedDB 全索引检查依赖 WebKit 的 IDBKEY 比较器，通用比较器在备份和迁移后均产生相同索引误报，未以此声称完成原生索引检查。

本机草稿比后端新 26 个操作修订，差异包含视频节点计时。已分别保留，并映射保存基线修订，使其仍能通过正常串行保存恢复，不丢弃草稿。

## 实现与自动化验证

- Rust 媒体写入在持久引用返回前完成文件同步、哈希核对；Agent dry-run 只推导引用，提交时才写文件。
- 图片结果在进入节点前逐份落盘，层分解结果同样处理；原始 data URL 不留在任务结果或撤销快照。
- JSON 语义比较不产生完整序列化副本；保存检查点与缓存请求合并。人工操作不再生成随后丢弃的 Agent 撤销快照。
- 首页摘要保留节点/连线计数与视图，正文只按需读取，读取顺序执行。摘要禁止保存为正文；改名和导出先加载完整项目。
- CLI `projects status <id>`、`projects node <id> <node-id>` 与 API 项目 action `status` / `node` 提供修订和按需内容；MCP `canvas_read` 可选 `summary: true`。已有 tasks get 提供任务状态，旧接口保持兼容。
- 保存、恢复、冲突、页面同步、无二次确认和连续编辑检查点：37 项通过。画布协议/store/Omni Flash：27 项通过。TypeScript 通过。Rust Adapter 50 项通过；桌面 Rust 66 项通过、3 项既有忽略。日志保留于备份目录。

## 原生与安装

隔离 App 使用同样的 108 节点、92 连线、84 原图；独立 identifier/端口、无渠道凭据。仅隔离副本将一条旧视频 loading 状态中和，防止原任务阻塞验收队列，正式节点未作此改动。

原生画面确认图片可见；通过正式 CLI 派发到原生画布执行器导入图片、保存、撤销、重做、再撤销，节点数为 108 → 109 → 108 → 109 → 108，回执与数据库一致。正常退出、重新打开后修订不变，仍为 108 节点、92 连线；保存正文无内嵌 data image。此为原生执行器闭环，未声称所有手工拖放和快捷键均实测。

- 同规模加载与静置约 417 秒：WebKit peak footprint 1.50 GiB，最后一分钟约 493–668 MiB；系统 swapout 增量为 0。
- 导入/撤销/重做与重启窗口约 124 秒：WebKit peak footprint 1.75 GiB，末次约 638 MiB；系统 swapout 增量为 0。
- 原现场 peak 29.9 GiB 来自历史同机实测，本轮没有重跑旧版。测量范围是指定窗口，不代表无限期泄漏证明，也不将系统总 swap 全部归因于本应用。

正式离线迁移已执行并读回：其他 30 个项目和 170 条非目标 IndexedDB 记录逐字节一致，84 原图哈希一致。22 条旧 queued/running 任务完整备份后仅状态改为 interrupted，未删除请求、结果或历史。保留原先更晚的 op revision 740 草稿，启动后正常恢复保存；随后原有视频任务 `bfc4d0cf-e665-46c6-bd14-e4f43b6e49c9` 查询返回成功，自动回填结果到既有节点，未创建新的生成任务。原审计限额在正常两次保存时淘汰两条较早人工操作，迁移前完整记录仍在备份及迁移证据中。

正式 App 已通过 `desktop build:app` 安装至 `~/Applications/小陈的画布.app`，identifier 保持 `com.chenyuxiaojin.infinitecanvas`，本地签名校验通过，BUILD_ID 为 `2Nso3DDbOChwqKjcfyVRE`。旧 App 与隔离验收 App 均压缩、验证后移入废纸篓；正式构建包被移动安装，无额外可运行包。正式原生首页计数、原图和“已保存”已确认；渠道配置字节未变。正式手工工具栏新增文本、双击改名、按钮撤销/重做均与数据库一致，独立验收项目 `memory-save-native-20260920` 留作复验。正常退出后主 App、Node、Rust API 均退出；冷启动后测试项目与原项目的正文/修订分别逐字一致，原生显示正确文本和“已保存”。7 个渠道的整个配置记录逐字节一致，Omni Flash 保留。

正式约 355 秒窗口包含冷启动、原 108 节点画布、手工编辑/撤销/重做、退出重启、再次打开原画布。WebKit lifetime peak footprint 为 2.00 GiB（5 秒采样观测最大值 1.61 GiB），末分钟包含切换页面为 137–491 MiB，系统 swapout 增量为 0，系统 swap 使用量 5032 → 5016 MiB。最终停留原画布，108 节点、92 连线、“已保存”；30 个其他既有项目正文保持一致。仅新建上述 1 个明确命名的测试项目，没有清理旧项目或素材。

`~/Applications` 仅有一个画布 App，本工作树无剩余 `.app` 构建包；原生应用枚举只有正式画布在运行，无独立 Node 入口，Node/Rust 子进程均无终端并由正式 App 托管。正式签名、安装日志、备份清单和测量原始数据均保留在备份根目录。

本轮没有真实付费生成；新图片结果媒体外置路径使用自动化结果桩和 Rust 落盘测试验证。长期复杂编辑、多画布并行和所有媒体格式不在本轮有限窗口内。
