---
title: TODO
description: 当前项目后续值得处理的事项
---

# TODO

- 本机外置原图生成输入与独立命令版本误判已补齐，转入 `pending-test.md`；继续保留真实供应商生成质量和服务端限制的验收，不以本机协议替身代替真实成片。

- 内嵌媒体与保存内存修复已完成正式安装及有限窗口原生验收，已移至 `pending-test.md`；后续仅继续观察长时间、多画布复杂编辑，不以约 6 分钟结果替代长期稳定性。

- 聊天首次使用目录引导已实现并通过 23 项相关测试，已正式安装且原生选择框/取消通过；成功绑定后的真实模型续跑待验证，见 `pending-test.md`。

本文档用来记录当前项目后续比较值得处理的事项。

- Laogou Seedance 2.5：媒体接口已正式接入。独立无人环境测试仍在上游运行，待验证实际成片下载和画布播放；原人物剧情因 SD2.5 肖像限制未生成，不把接口受理当作人物成片成功。详见 [接入实测](laogou-seedance-integration.md)。

- 当前已授权全部修复、优化、Agent 接入及 Go → Rust 迁移。统一范围和完成标准见 [产品需求与验收标准](../overview/product-requirements.md)，由执行任务逐项闭环；下方历史“待授权”表述不能覆盖用户后续已授予的权限。

- 本轮修复、七项优化与 Rust API 已安装到正式 App，保存精度误报已消除，原表与素材读回通过。剩余正式操作及证据统一见 [整合验收](canvas-rust-repair.md)，实现完成不等于全部产品验收通过。
- 原生续验已补多选/新连线/越界、长聊天、冲突/忙碌恢复、恢复后重启和上下文/SOP阈值。Kling真实组件40项、原文件URL生命周期通过。CUA原生drag按钮状态为0的工具限制已通过正式事件定位，无需改产品保护；性能收益未证实、WebKit回收、Kling正式布局与额外真模型单列。完整交付未全部验收，见[原生续验](canvas-native-acceptance.md)。
- Codex 既有一条真实只读消息、Grok 专项一条真实工具调用通过；Grok/Antigravity 接入已安装。Antigravity 原单次消息失败，修复后零模型初始化/恢复通过，真实工具未复验。连续对话、图片引用、取消/压缩/生成/删除确认仍有未测范围，不追加付费验收，见 [接入记录](canvas-local-agent-integration.md)。

- 本地工作流收敛已按授权正式安装并重启；11 项本地工作流、32 项原图、5 项安装器检查通过，原生入口/画布原图抽验/终端/绑定/提示词及数据对账见 [安装验收](local-workflow-install-acceptance.md)。剩余限制为原生全程请求抓包、远程全文/新收藏和实际 AI 生成，本轮未消耗额度。
- 原数据库的用户、服务端素材和工作流记录、浏览器旧会话与存储配置原样保留。若后续需要把服务端独有素材开放为本机收藏或处置旧账号数据，须单独确认具体范围和备份，不自动迁移或清理。
- 左栏第 3 版已打包安装并验证分类、折叠、搜索及点击定位；待原生窗口捕捉恢复后，继续 `pending-test.md` 中的完整视觉对照和剩余交互回归。
- 「小陈的画布 1.0.0」独立验收未整体通过，见 [独立验收报告](independent-macos-acceptance.md)：有限终端输出/输入与原数据库保护已有实测，其余状态按 `pending-test.md`。
- 用户授权的三步修复已正式安装，详见 [顺序修复验收](ordered-repair-acceptance.md)。32 张真实图片读取/哈希/完整解码通过，原生节点和放大预览抽验通过；仍待 32 张逐张视觉、离屏内存释放和所有辅助工具，参考缩略图/下载/裁剪未扩展。
- 案例 2/3/4 六处旧 App MCP 路径已更新并只读连通，三个原绑定逐字不变，标题匹配保护已安装。最终只有一个可启动 App，4 条旧包历史均在废纸篓且禁止启动；Spotlight/应用枚举唯一，无独立 Node。备份保留，不为清除禁用历史记录永久删除文件。
- 终端背压和异常清理、累计 ACK 均已安装；同参数原生 64 MiB 输出 98.43→7.01 秒，无错误。高输出输入/拖宽、关闭、重开、退出和重启已通过，前端 60/Rust 37 项通过。仍待原生后台恢复、长期复杂进程树和真正帧时间；保留普通 Shell，不恢复 AI 品牌启动按钮。
- 后续验收真实媒体下的平移、缩放、拖节点和帧时间；检查 UI-only 状态保存、连续平移可见集合及全连线扫描。60 节点 ZIP 已提供但尚未成功导入，不能记作实测通过。
- 终端有限高流量已测并加消费确认；后续仍需区分画布操作与模型响应的卡顿，不能把终端输出收益当作整个应用或 AI 提速。
- 提示词按需加载改造已移至 `pending-test.md`；后续如需清理历史订阅全文，应单独确认保留范围及备份方案，不自动删除。
- 单一应用入口与 Node 静默启动已移至 `pending-test.md`；后续本机更新使用 `desktop` 的 `build:app` / `install:app`，备份保留为 ZIP，不再散放可运行 App。

分支整合已完成代码比对，新增原生验收范围见 [待测试](pending-test.md) 和 [分支核对](canvas-branch-integration.md)。原有产品待办不因分支合并而标为完成。

## 完整程序操作与人机共同编辑

统一依据见 [PRD R9](../overview/product-requirements.md#r9--完整程序操作与人机共同编辑)，逐项覆盖和当前证据见 [R9 实施与验收](canvas-r9-integration.md)。通用节点、项目历史、传输与生成任务已实现，进入 pending-test。

- 手动拖入/粘贴/收藏及导演台发送素材已接统一记录；常用节点与素材操作、手动裁剪回查、导演台截图和 1 秒视频、重启和原数据核对已有正式证据，转入 pending-test。尚未穷尽的复杂时间轴、拖放、素材类型及并发交互继续专项验收。
- 在用户具体授权的范围内验收图片、视频、音频的真实新任务、输出文件及本机 Agent 工具回执；本轮没有追加付费模型调用。

## 七项优化的剩余整合

统一要求见 [PRD 第 9 节](../overview/product-requirements.md#9-已批准的七项优化)，已实现部分见 pending-test；数据专项证据见 [数据与请求优化](canvas-data-optimization.md)，前端专项见 [交互与素材优化](canvas-ui-optimization.md)。

- 本轮已补正式 ZIP 导入导出、持久历史恢复、撤销重做、UI-only 正常重启，以及并发/忙碌保护、分组跨边界和长聊天，见 [原生续验](canvas-native-acceptance.md)。此前窗口停点已续接完成，不再作为当前整体阻塞；真实连续操作和性能仍需补证。
- 在正式 App 的同一真实素材场景比较拖动连线、长聊天和临时素材内存；保留原文件与查看画质，记录硬件、规模、时长和指标。
- 原生断开/慢响应/流式对话/长视频及服务退出的端到端取消验证；HTTP 媒体取消测试不能代替所有本地 IPC 中途取消能力。
- SOP 来源指纹、70%/85%及未知上限显示已原生验证；模型实际收到的输入、后续真实用量事件与 Antigravity 修复后的真实工具调用仍需另行取证，不追加未授权付费调用。

- 旧 URL 与全局素材库释放、长对话首次按页挂载的源码已完成并安装，已移至 pending-test。正式 App 原图加载、历史翻页/搜索/完整复制已验；实时新回复跟随、WebKit 回收及实际内存收益仍待验收。

- 平移正式诊断已闭环：CUA drag传入buttons=0，capture成功后被正常松手保护结束；无产品修复，临时插桩全部撤销并恢复普通正式包。小地图视口移动通过，原数据/素材与签名复核通过；可作为保留验证边界的修复合并，不宣称完整性能或真模型全验，详见[原生验收结论](canvas-native-acceptance.md)。

## 合并后的专项测试

用户已要求先提交合并，再继续以下四项；合并代码不等于这些项目已经通过。

- 性能：已测 20 张原图与音视频的 WebKit 程序驱动基线，p95 为 21–22 ms；仍有 366–904 ms 偶发长帧，不宣称稳定 60 fps 或提速，见 [运行时专项](canvas-runtime-validation.md)。
- 界面内存回收：两轮实际项目退出后 Blob 缓存归零，重进 20 份原图字节/哈希一致；普通包另测自然 RSS 变化，不能把 Blob 释放等同于所有解码内存立即释放，见运行时专项。
- 音频实际输出：单 App 系统输出实测通过，0.5 秒测试信号与原音频相关系数 0.99997。未采集麦克风或其他应用，不宣称物理扬声器声学验收。
- 模型调优：已修正 16 项上下文上限挤掉当前选择/引用的问题，输入大小不增加；48 项相关回归通过，普通包已安装。真实短只读调用按本轮既有订阅授权单独记录，旧轮次数限制不覆盖本轮新请求。

- 本轮真实模型结果：Codex/Grok 各一次摘要工具通过；Antigravity 非法内置工具名已修复并安装，6 项 Rust 回归通过，但复验仅得到一次模型文字/JSON 回复、实际画布工具 0 次，仍未通过工具验收。工具暴露与 JSON 兼容路径的根因尚未区分，不追加本轮模型调用。
